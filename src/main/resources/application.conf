project {
  assets {
    assetA = "BTCUSDT_250627"
    assetB = "BTCUSDT_250926"
  }
  maxHistorySize = 10
  spreadThreshold = 100000
  assetLoadingDelay = "15 seconds"
}

postgres {
  connectionString = "jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/postgres?user=${POSTGRES_USER:postgres}&password=${POSTGRES_PASSWORD:postgres}"
}

telemetry {
  otelEndpoint = "${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}"
  logLevel = "${LOG_LEVEL:DEBUG}"
}

cache {
  priceExpiration = "30 seconds"
  spreadExpiration = "30 seconds"
}